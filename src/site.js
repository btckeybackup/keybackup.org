// Single source of truth for anything that appears in more than one place.
// Bump DOC_VERSION when a new document is released; the header, the download
// button, and the filename all follow from here.
export const DOC_VERSION = '1.2'
export const DOC_FILE = `keybackup-v${DOC_VERSION}.html`
export const DOC_HREF = `/${DOC_FILE}`

export const REPO_URL = 'https://github.com/btckeybackup/keybackup.org'
